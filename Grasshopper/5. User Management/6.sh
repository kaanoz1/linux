echo Vim ツ.
echo User Management Tools.
echo
echo On one machine there might be multiple users.
echo You can add users by command: adduser or useradd.
echo Example: sudo useradd az. The user az will be created.
echo Entries for az in files /etc/shadow and /etc/passwd will be added.
echo For remove users use: userdel. I couldnt.
echo To change the password of user. Use passwd username. Ex: passwd prag. If you are root. You can do this.
