echo /proc filesystem.
echo
echo Everything is a linux is a file. Even processes. Process information is stored in a special filesystem known as the /proc filesystem.
echo "ls /proc | head -n 15 | nl"
ls /proc | head -n 15 | ns
echo
echo 
