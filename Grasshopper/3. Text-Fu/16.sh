echo Command: grep "( )"
echo
echo grep finds the given pattern in that file.
echo Inspect: grep me .herheart
echo RegEx can be used with grep. Inspect: grep 'A*' file.txt
echo Inspect the output of command: "env | grep -i User '(-i for searching case INsensitive)'"
echo
env | grep -i User
echo