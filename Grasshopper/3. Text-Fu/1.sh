echo stdout
echo echo command takes input in stream of stdin "(Standard Input)" and prints it to stdout "(Standard Output)"
echo Inspect this command: echo "Hello World" > output.txt
echo
echo Here, > operator is used to redirect the stdout of echo command to file output.txt.
echo If output.txt already exists, it will be overwritten.
echo If it doesn't exist, it will be created.
echo
echo
echo If you don't want to overwrite the file, use >> operator to append.

