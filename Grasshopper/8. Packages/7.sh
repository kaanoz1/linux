echo Compile Source Code
echo
echo You might install the source code of application. To run this:
echo
echo First off. download the compiler: sudo apt install build-essential
echo then open the source code which probably compress in .tar.gz: tar -xzvf package.tar.gz.
echo
echo Parameters: x: extract, z: in the format of gzip, v: verbose, f: file.
echo
echo Most of source code has file ./configure which checks for dependencies.
echo
echo run: make which checks for Makefile.
echo
echo install make: sudo make install.
echo to remove make: sudo make uninstall.
echo
echo instead of installing make you might use another safe option which is sudo checkinstall.
echo
echo 
