echo rpm and dpkg
echo
echo for Debian: $ dpkg -r some_deb_package.deb
echo RPM: $ rpm -e some_rpm_package.rpm
echo
echo Debian: -r for remove.
echo Red Hat: -e for erase.
echo
echo List installed packages:
echo
echo Debian dpkg -l
echo RPM: rpm -qa
echo
echo Debian: l for list
echo RPM: q for query and a for all
echo
