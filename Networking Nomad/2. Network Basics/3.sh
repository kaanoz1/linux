echo "TCP/IP model"
echo "Application layer:"
echo "  The top layer of the TCP/IP model: This layer uses HTTP and SMTP."
echo "    It determines how your computer's programs (such as your web browser) interface with the transport layer services to view the data that gets sent or received."
echo
echo "Transport Layer"
echo "  How data will be transmitted, includes checking the correct ports, the integrity of the data and basically delivering ou packets"
echo "    This layer uses TCP (Transmission Control Protocol) or UDP (User Datagram Protocol)"
echo
echo "Network Layer"
echo
echo "  This layer specifies how to move packets between hosts and across networks."
echo "    This layer uses: IP or ICMP (Internet Control Message Protocol). ICMP helps tell us what is going on, such as error messages and debugging information."
echo
echo
echo "Link Layer"
echo "  This layer specifies how to send data across a physical piece of hardware, such as data traveling through Ethernet, fiber, etc."
echo
