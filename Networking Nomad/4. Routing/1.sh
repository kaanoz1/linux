echo "What is a router?"
echo
echo "If a router has the ip address checking by looking its ARP look up table. It redericts it to there"
echo "If now, uses default gateway."
echo
echo "Packet SWITCHING is basically receiving, processing, and forwarding data to the destination device."
echo "ROUTING is a process of creating the routing table so that we can do SWITCHING better."
echo "Before routing, FLOODING was used. If a router doesn't know which way to send a packet, then every incoming packet is sent through every outgoing link except the one it arrived on."
