echo SWAP
echo
echo What is this swap partition? Well, swap is what we use to allocate virtual memory to our system.
echo "If you are low on memory (like me ツ) the system uses this partition to 'swap' pieces of memory of idle processes to the disk, so you're not bogged down for memory."
echo
echo